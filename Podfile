# Uncomment this line to define a global platform for your project
platform :ios, ‘8.0’

target ‘1 Gallon’ do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for 1 Gallon
  pod "Timepiece"
  pod 'SCLAlertView'

  target ‘1 GallonTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target ‘1 GallonUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
